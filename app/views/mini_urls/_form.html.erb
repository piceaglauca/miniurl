<%= form_with(model: mini_url, local: true) do |form| %>
  <% if mini_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mini_url.errors.count, "error") %> prohibited this mini_url from being saved:</h2>

      <ul>
      <% mini_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :full_url %>
    <%= form.text_area :full_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
